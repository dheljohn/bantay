import 'package:flutter/material.dart';

class WebEntrypoint extends StatelessWidget {
  const WebEntrypoint({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(home: Text('Web Entrypoint'));
  }
}
